import React from 'react';
import { Media } from 'react-bootstrap';
import moment from 'moment';

function MessageTest({ message, user }) {
  return (
    <Media>
      <Media.Body>
        <div>test</div>
      </Media.Body>
    </Media>
  );
}

export default MessageTest;
